import VueImageGallery from "./VueImageGallery"

export default {
  // eslint-disable-next-line no-unused-vars
  install(Vue, options) {
    Vue.component('vue-image-gallery', VueImageGallery)
  },
};
